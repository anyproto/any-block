# JavaScript support

This directory is reserved for a native JavaScript codec or Go/Wasm wrapper.
Its public API should be driven by the same conformance fixtures as the Go
codec so implementations cannot silently diverge.
